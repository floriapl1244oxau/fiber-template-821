version: '3.8'
services:
  app:
    build: .
    ports:
      - "4090:3877"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: fiber_template_821
      POSTGRES_PASSWORD: changeme
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
